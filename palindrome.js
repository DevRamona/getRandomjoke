function isPalindrome(x) {
    const lowerCaseX = x.toLowerCase()
    const reversedString = lowerCaseX.split("").reverse().join("") 
  //   if(lowerX !== newStr) {
  //     return false;
  //   } else {
  //     return true;
  //   }
    return lowerX !== newStr ? false : true
  }
